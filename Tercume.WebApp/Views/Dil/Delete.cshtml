@using Tercume.WebApp
@model Tercume.Entities.Dil

@{
    ViewBag.Title = "Dil Sil";
}

<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <h2>@ViewBag.Title</h2>
        <h3>Silmek istediğinize emin misiniz?</h3>
    </div>
</div>

<div class="row">
    <div class="col-md-8 col-md-offset-2">

        <h4>Dil</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Dil_isim)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Dil_isim)
            </dd>

            

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                @Bootstrap.Button("danger", string.Empty, "submit", "remove", " Kaldır")
                @Bootstrap.LinkButton("/Dil/Index", "default", string.Empty, string.Empty, "arrow-left", " Back to List")
            </div>
        }

    </div>

</div>
